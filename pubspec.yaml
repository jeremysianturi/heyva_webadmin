name: heyva_web_admin
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev.
version: 1.0.0+1

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: 4.6.5
  dio: ^5.0.3
  loading_overlay: ^0.3.0
  get_storage: ^2.1.1
  flutter_svg: ^1.1.2
  firebase_core: ^2.9.0
  firebase_auth: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:

  uses-material-design: true
  assets:
    - assets/images/

resources:
  - string.dart
  - color.dart

